<style>
    .container-fluid{
        width: 80%;
    }
    .navbar{
        border-radius: 0px;
        margin-bottom: 0px;
        font-size: 18px;
        font-weight: 600;
    }
</style>
<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <%=link_to "HiLove",root_path,{class: "navbar-brand"} %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li>
          <%=link_to "图片上传",new_file_management_path%>
        </li>
      </ul>
      <ul class="nav navbar-nav">
        <li>
          <%=link_to "图片展示",file_managements_path,method: :get%>
        </li>
      </ul>
      <ul class="nav navbar-nav">
        <li><a href="javascript:void(0);">联系我们</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
        <li class="dropdown">
          <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
            <%= current_user.username.to_s.capitalize %><span class="caret"></span>
          </a>
          <ul class="dropdown-menu">
            <li><%= link_to('登出', destroy_user_session_path, :method => :delete) %></li>
          </ul>
        </li>
      <% else %>
        <li><%= link_to('注册', new_registration_path(:user)) %></li>
        <li><%= link_to('登录', new_session_path(:user)) %></li>
      <% end %>
      </ul>
    </div>
  </div>
</nav>